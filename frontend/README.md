# Euchre Frontend

React/Vite client for the single-player Euchre backend.

## Run Locally

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8080`.

The backend and frontend run separately. Spring Boot does not serve the frontend.
